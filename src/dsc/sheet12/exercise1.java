/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package dsc.sheet12;

import StdLib.*;

/**
 * Schreiben Sie eine statische Methode, welche zwei Double-Werte ymin / ymax (mit ymin < ymax) 
 * und ein Double-Feld a als Argumente übergeben bekommt und mit Hilfe der StdStats-Bibliothek die 
 * Werte in a so linear skaliert, dass alle bei gleichem Verhältnis zwischen ymin und ymax liegen.
 *
 * @author david
 */
public class exercise1 {
    
    public static double[] scaleA (double ymin, double ymax, double[] a) {
//        a[i] = a;
        return a;
    }
    
    
    
    public static void main(String[] args) {
        
        double ymin = Double.parseDouble(args[0]);
        double ymax = Double.parseDouble(args[1]);
        
    }
}
